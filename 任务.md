---
tags:
  - type/dashboard
---
> [!info] 这里是任务中心
> 当前 Tasks 设置把 `#task` 作为全局过滤条件：下面每个 `tasks` 查询都会先限定到带 `#task` 的复选框，再应用 `not done`、截止日期等局部条件。任务可以写在任何笔记中，不必全部集中在本文件。

## 快速添加

```markdown
- [ ] 编写日志模块测试用例 #task ⏫ 📅 2026-08-01
```

- `📅`：截止日期。
- `🔺 / ⏫ / 🔼 / 🔽 / ⏬`：最高、高、中、低、最低优先级。
- 没有日期也可以创建任务，Dashboard 会将它归入“无截止日期”。
- 建议一个任务写成可执行动作，例如“整理链接脚本案例”，不要只写“链接脚本”。

### 什么时候添加 `#task`

- 希望任务出现在 Dashboard、任务中心和 Tasks 查询中：添加 `#task`。
- 只是当前笔记里的阅读进度、练习清单或临时检查项：不添加 `#task`，它会保留为普通复选框。

```markdown
- [ ] 8 月 1 日前完成日志模块测试 #task ⏫ 📅 2026-08-01
- [ ] 阅读本章的“整型提升”小节
```

## 按截止日期

```tasks
not done
has due date
sort by due
sort by priority
hide tags
```

## 所有未完成任务

```tasks
not done
sort by priority
sort by due
group by folder
hide tags
```

## 已完成任务

```tasks
done
sort by done reverse
limit 20
hide tags
```
